<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Edit Bus - Admin Panel</title>
<link rel="stylesheet" href="/stylesheets/style.css" />



</head>
<body>
  <nav class="navbar">
    <div class="nav-left">
      <img src="/images/logo.png" alt="Logo" class="logo" />
      <b>Pokhara Bus Routes</b>
    </div>
    <div class="nav-center">
      <h1>Edit Bus</h1>
    </div>
    <div class="nav-right">
      <button class="btn btn-danger">LogOut</button>
    </div>
  </nav>

  <div class="container">
    <aside class="sidebar">
      <ul>
        <li><a href="/buses">Show Buses</a></li>
        <li><a href="/buses/add">Add Buses</a></li>
      </ul>
    </aside>

    <main class="main-content" style="max-width: 400px; margin: auto;">
      <form method="POST" action="/buses/saveEdited/<%= bus._id %>">
        <label for="busNumber">Bus Number:</label>
        <input
          type="text"
          id="busNumber"
          name="busNumber"
          value="<%= bus.busNumber %>"
          required
          min="1"
        />

        <label for="pickUp">Pickup Point:</label>
        <input
          type="text"
          id="pickUp"
          name="pickUp"
          value="<%= bus.pickUp %>"
          required
        />

        <label for="destination">Destination:</label>
        <input
          type="text"
          id="destination"
          name="destination"
          value="<%= bus.destination %>"
          required
        />

        <label for="departureTime">Departure Time:</label>
        <input
          type="time"
          id="departureTime"
          name="departureTime"
          value="<%= bus.departureTime.toTimeString().slice(0, 5) %>"
          required
        />

        <label for="fare">Fare:</label>
        <input
          type="number"
          id="fare"
          name="fare"
          value="<%= bus.fare %>"
          required
          min="0"
        />

        <button type="submit" class="btn btn-primary">Save</button>
      </form>
    </main>
  </div>
</body>
</html>



